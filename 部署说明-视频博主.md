# Horizon AI 新闻雷达：视频博主版部署说明

更新时间：2026-09-19

## 已完成的配置

- 使用 `ai-creator` 作为默认处理 Profile，并改造成“AI 视频选题雷达”。
- 每条重要新闻可输出：新闻摘要、时效性、视频角度、3 个标题、开场钩子、视频结构、画面素材和事实核查提醒。
- 默认生成简体中文日报。
- 信息源包括 OpenAI、Google AI、Google DeepMind、Hugging Face、NVIDIA、TechCrunch、GitHub、Hacker News、Reddit、Telegram、OSS Insight 和 Google News。
- Twitter 默认关闭，因此不需要 Apify 密钥。
- 每日最多保留 12 条高价值选题，降低 API 消耗和信息噪音。
- GitHub Actions 每天北京时间约 07:15 自动运行，也支持手动运行。
- 结果自动部署到 `gh-pages` 分支，可通过 GitHub Pages 浏览。

## 你需要准备的内容

### 1. GitHub 账号

需要使用你的 GitHub 账号创建仓库并授权 GitHub CLI。授权必须由你本人在 GitHub 页面确认。

### 2. DeepSeek API 密钥

当前配置使用 DeepSeek。密钥名称必须是：

```text
DEEPSEEK_API_KEY
```

不要把真实密钥提交到仓库，也不建议粘贴到聊天中。正确做法是保存到 GitHub 仓库：

```text
Settings → Secrets and variables → Actions → New repository secret
```

名称填写 `DEEPSEEK_API_KEY`，值填写你的 DeepSeek API 密钥。

GitHub 自动提供的 `GITHUB_TOKEN` 已经接入工作流，不需要另外创建个人访问令牌。

## 发布到 GitHub

完成 GitHub 登录后：

1. 创建一个新仓库，推荐名称：`horizon-ai-news-radar`。
2. 将本地项目提交并推送到 `main` 分支。
3. 在仓库 Secrets 中添加 `DEEPSEEK_API_KEY`。
4. 打开 `Actions`，启用工作流。
5. 手动运行 `Daily Horizon AI News Radar`，确认首次执行成功。
6. 打开 `Settings → Pages`。
7. 将发布来源设置为 `Deploy from a branch`。
8. 选择 `gh-pages` 分支和 `/ (root)` 目录。

首次工作流成功后，日报站点通常位于：

```text
https://你的GitHub用户名.github.io/horizon-ai-news-radar/
```

## 可选推送

当前 Webhook 默认关闭。如果后续需要将日报推送到飞书、钉钉、Slack 或 Discord，再配置：

```text
HORIZON_WEBHOOK_URL
```

并修改 `data/config.github.json` 中的 `webhook.enabled` 和 `webhook.platform`。

## 主要配置文件

- `data/config.github.json`：模型、信息源、筛选数量和日报设置。
- `profiles/ai-creator/`：视频选题评分和内容策划提示词。
- `.github/workflows/daily-summary.yml`：每日自动运行与 Pages 部署。

## 成本控制建议

- 先保持每日 12 条上限，观察一周 API 消耗。
- 如果新闻太多，将 `threshold` 从 `7.5` 提高到 `8.0`。
- 如果新闻太少，将 `threshold` 降低到 `7.0`。
- 如需减少分析数量，可关闭部分 RSS 或 Google News 信息源。
